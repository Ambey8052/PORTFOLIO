# Comma-separated list of allowed frontend origins for CORS.
# Example: https://your-portfolio.vercel.app,http://localhost:5173
FRONTEND_URL=

# Render sets PORT automatically in production; only needed for local overrides.
# PORT=5000
